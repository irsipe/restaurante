{% extends 'base.html.twig' %}

{% block body %}

    <body>
        {{parent()}}

      <article>
            <div class="dishe_img">

            </div>
            <div class="dish_info">
                <h1 class="item_name">   Carta</h1>
                {% for cargo in carta %}
                     <h3 class="item_desc">{{ cargo.nombre_es }}&nbsp&nbsp &nbsp &nbsp   </h3>
                    <a href="#"><img src="{{cargo.imagen}}" alt="" title=""/></a>
                   <h3 class="item_desc">  {{ cargo.descripcion_es }}</h3>
                {% endfor %}
            </div>

        </article>
            
        {% include 'footer.html.twig' %}

    </body>
</html>



{% endblock %}